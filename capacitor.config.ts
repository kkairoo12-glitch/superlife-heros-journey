import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.superlife.hero',
  appName: "Superlife Hero's Journey",
  webDir: 'dist'
};

export default config;
